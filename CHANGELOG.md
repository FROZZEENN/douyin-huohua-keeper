# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 修复误报与调整通知策略（2026-09-23）

**修复：发送成功却报「结果不确定」**

恒定制文案的账号下会同时踩上三个坑，导致确认逻辑的判据**集体失效**：

1. 会话列表预览**本来就是同一句**（昨天发的那条）→「预览变了」失效；
2. 会话**本来就在列表第 1 位**（昨天发过、今天没人再聊）→「跳到第 1 位」失效；
3. 消息区是**虚拟列表**，新增一条的同时挤掉一条 →「条数变多」失效。

于是消息明明发出去了，却每天有 1~2 个人被报「不确定」。

新增一条**锚点判据**：发送前给「最后一条消息」打一个记号，发送后只要它**后面**
出现了新节点，就说明消息区确实多了一条 —— 不看内容、也不看总条数，
上面三个坑同时踩上也不影响。（旧的三条判据保留，作为不能打锚点时的兜底。）

**变更：每日汇总只在出问题时推**

- 白天正常运行成功 → **不再推送**（消息已经发给好友，不需要报喜）；
- 只有今天出过问题（没跑 / 没成功 / 有失败 / 有「结果不确定」）→ 23:30 推一条提醒；
- 按**天**累积判断，而不是只看最后一次运行 —— 早上成功、下午手动那次挂了，同样会提醒。


### 公开前收尾（2026-09-19）

**新增**

- 拦掉用不上的页面资源（默认 `media,font`）：一次任务少拉回约 20MB 入网流量、加载更快 —— `HUOHUA_BLOCK_RESOURCE_TYPES`
- Chromium 沙箱按环境决定：**容器里关、桌面保留**（桌面不该白白牺牲沙箱）—— `HUOHUA_CHROMIUM_NO_SANDBOX`
- 令牌连续输错 5 次 → 暂停 60 秒（429 + `Retry-After`），认证成功即清零
- 构建镜像时可指定下载源，国内 / 境外都顺：`APT_MIRROR` / `PIP_INDEX_URL` / `PLAYWRIGHT_DOWNLOAD_HOST`
- 新增 `CODE_OF_CONDUCT.md`、`.editorconfig`、功能建议 Issue 模板

**变更**

- 登录页中继截图画质 60 → 45、刷新 6s → 15s，**页面切到后台自动停**（出网流量约降 8 成）
- 文档：新增「开始之前」（前置条件 + 国内网络加速）与独立的「免责声明」
- README：补工作台截图与目录

**修复**

- 运行时报错提示漏了 `chromium-headless-shell`，照着做会陷入死循环（3 处）
- CI：Docker 检查被自己的 entrypoint 打败（每次必红）；`test_atomic.py` 两个用例在 Windows 上 flaky
- README 里 Docker 徽章的锚点指向了已改名的标题（坏链接）

**移除**

- 两个从未被任何代码消费的配置项：`HUOHUA_CAPTURE_ON_ERROR`、`HUOHUA_TRACE`

### 真机验证（2026-09-15）

S3 里程碑在**真实抖音账号**上首次跑通，全链路：

```
加载登录态 → 导航会话页 → 读取会话列表(15 条) → 匹配收件人 → 打开会话
→ 校验会话头部名字 → 输入消息 → 点击发送 → 确认状态机判定送达
```

结果：`status=success，detail=会话列表已更新（用时 1.3s）`，未触发重试。

这次真机验证暴露并修掉了 11 个**只在真实页面上才会出现**的问题，集中体现为四类根因：

| 根因 | 表现形式 | 修复思路 |
| --- | --- | --- |
| **抖音的 class 是构建哈希名** | 二维码、会话列表容器、会话名、预览、会话头部、发送按钮这 6 处选择器**全部命中不了** | 一律改用语义标识：`data-e2e` / `aria-label` / 有意义的固定类名（如 `RightPanelHeadertitle`、`.e2e-send-msg-btn`） |
| **时序：`domcontentloaded` 早于前端渲染** | 导航后立即检查元素 → 稳定误判「找不到二维码 / 认不出会话页」 | 所有「导航后查元素」改为**轮询到截止时间** |
| **顺序：元素存在性有前置依赖** | 消息输入框**要打开会话后才出现**，旧实现却在打开会话之前检查它 → 每个收件人都失败并误报「登录态失效」 | 重排为：页面就绪 → 打开会话 → 输入框就绪 → 输入 |
| **判空失效** | 编辑器里永远有一个含 U+200B 的 `data-enter` 节点，`inner_text().strip()` 去不掉 → 「输入框还有内容」永远成立 → **误报失败 → 触发重试 → 重复发送** | 判空一律先剔除零宽字符 |

其他修正：

- `LOGIN_URL` 由首页 `/` 改为 `/chat`（首页的登录弹窗在截图时二维码还没渲染出来）
- 新增形状校验：二维码必然接近正方形，用它挡掉「截到登录弹窗标题横幅」这种误伤
- 新增 PNG 校验：拒绝空白占位图与非正方形截图，**宁可报错也不返回一张不是二维码的图**
- `poll_login` 新增 `SCANNED` 状态识别（判据：语义选择器不再命中 + 容器文案含「需在手机上进行确认」）
- 删除 `poll_login` 里「URL 在站内就算成功」这条判据 —— 它会把「只扫码未确认」误报成登录成功
- `save_storage_state` 增加会话 cookie 强校验（必须含 `sessionid` 等），拒绝存下无效登录态

### 新增

- `scripts/login.py`：**手动扫码登录助手**。弹出可见浏览器窗口以完成二级身份验证，
  终端实时打印状态变化，成功后自动原子写入登录态。首次登录必须用它（或等价的有头环境）。
- `workbench/static/scan.html`：专用扫码页。二维码每 2 秒自动同步刷新，
  并实时提示「等待扫码 / 已扫描请去手机确认 / 登录成功」。

### 重要前提修正

原先假设「扫码一次之后全自动」，实测证明**不成立**：

1. 抖音对「陌生设备 + 自动化浏览器」会要求**二级身份验证**（短信 / 刷脸 / 密码），
   这一步必须由人在**有图形界面**的环境完成，程序无法代劳
2. 因此 **Docker / 云服务器无法独立完成首次登录**，需本地登录后拷贝 `state.json`
3. 登录态失效后需要**再走一次同样的人工流程**

这三点已写入 README 的显著位置 —— 它直接影响「无人值守运行」这个目标的可达性，
不能含糊带过。

### 测试

- 新增 `tests/unit/test_qrlogin.py`（37 例）：二维码定位、形状校验、PNG 校验、
  会话 cookie 判定、`poll_login` 不误报
- 新增 `tests/unit/test_send_chain.py`（34 例）：零宽空格判空、输入框与打开会话的先后依赖、
  页面就绪轮询、容器可选、名字取最短、选择器表防退化
- 测试总数：**343 通过 / 3 跳过**

### 已完成

- S0 项目骨架
  - MIT 许可证、贡献指南、安全策略、PR 模板
  - `pyproject.toml`（ruff + pytest + coverage 配置）
  - 依赖清单与开发依赖清单
  - 目录结构：`engine` / `workbench` / `scheduler` / `notify` / `store`
  - 数据模型（`models.py`）：消息、联系人、账号、调度、运行报告、告警级别
  - 命令行入口（`__main__.py`）：serve / check / run-once / check-today / show-config
  - 环境自检脚本（`scripts/healthcheck.py`）：14 项检查，支持 JSON 输出
  - 开发辅助脚本（`scripts/dev.sh`）
  - Docker 多阶段构建 + Compose 编排 + 入口脚本
  - 部署辅助：systemd 单元、Nginx 反代、crontab 示例、备份迁移手册
  - GitHub Actions CI（lint / 测试矩阵 / 自检脚本 / Docker 构建）
  - Dependabot 配置
- S1 数据模型 · 配置 · 存储层（原子写、文件锁、运行历史）
- S2 浏览器管理 · 登录态有效性校验
- S3 会话定位 · 消息构建 · 发送确认状态机 ← **真机验证通过**
- S4 错误四分类 · 重试 · 两阶段防重复
- S5 五级分级告警 · 六通道投递
- S6 扫码登录模块（二维码渲染与轮询）
- S7 工作台后端 API
- S8 工作台前端页面
- S9 定时调度 · 错峰抖动

### 进行中

- S10 测试补全（scheduler / workbench 层）· 文档 · 教程

## 为什么从这个版本开始

在 0.1.0 之前，这个项目经历了一次方向调整：从「改造一个现成轮子」改为
「从零重写一遍」。原因不复杂 —— 想要的东西（网页工作台、云端扫码、分级告警、
发送确认）已经超出了任何现成方案的覆盖范围，改动量比重写还大，
而且改别人的代码时很容易继承一些自己都不理解的决策。

所以 0.1.0 是全新的起点。这里没有历史包袱，每个设计取舍都在文档里写明了理由。

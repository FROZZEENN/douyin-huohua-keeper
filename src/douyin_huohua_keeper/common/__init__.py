"""跨层共用的基础能力。

这些模块不隶属于任何单一子系统（engine / scheduler / workbench …），
而是被多处共享：

- ``errors``  —— 错误分类与重试决策。engine 的 sender 用它对单次发送失败
  归类，scheduler 的 runner 用它在跨收件人层面做「要不要重试 / 要不要中止」，
  工作台的运行详情页用它汇总失败分布。放在 engine 里会制造
  「engine 自己决定重试」的职责错位（engine 实际不做重试决策，只报告类别），
  所以独立成 ``common``。

后续若有别的跨层工具（数学、文本清洗、常量），也放这里，按模块细分。
"""

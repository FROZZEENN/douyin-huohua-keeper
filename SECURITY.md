# 安全策略

## 报告漏洞

请**不要**通过公开 Issue 报告安全问题。

用 GitHub 的 [Private vulnerability reporting](https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability)
功能，或者直接发邮件给维护者。

发出后请在 72 小时内留意回复。如果没收到，可以再发一次 —— 可能被邮箱拦了。

## 什么算安全问题

这个项目的敏感面比一般 Web 项目要窄，但也不是没有：

| 类型 | 例子 |
| --- | --- |
| **凭据泄露** | 登录态文件被写进了日志、错误信息里带了 Cookie、备份功能把助记词明文输出 |
| **鉴权绕过** | `HUOHUA_TOKEN` 校验可以被大小写/空值/特殊编码绕过；IP 白名单可以被 `X-Forwarded-For` 伪造 |
| **路径穿越** | 文件名参数可以跳出 `data/` 目录；上传的图片素材可以覆盖配置文件 |
| **远程代码执行** | 任何用户可控输入被送进 `eval` / `exec` / `subprocess` 而没做校验 |
| **SSRF** | 通用 Webhook 通道可以被用来探测内网 |
| **依赖漏洞** | 依赖链上有已知 CVE 且本项目确实会被影响 |
| **信息泄露** | 未鉴权接口暴露了配置内容、账号信息、运行历史 |

## 什么**不算**安全问题

这些是已知的设计取舍，不用报告：

- **抖音账号被风控 / 限制**。这是使用自动化工具的固有风险，README 里写明了。
- **登录态过期需要重新扫码**。这是抖音的机制，不是 bug。
- **无头模式下浏览器占内存较多**。Chromium 就是这样。
- **没有 TLS**。HTTPS 由反向代理负责，见 `deploy/nginx/`。
- **工作台在未设令牌时无鉴权**。这是有意的：本地试用不该被挡住。但自检脚本
  会警告，README 和 `.env.example` 都标注了必须设置。

## 我们的安全承诺

1. **不收集任何数据**。没有遥测、没有统计、没有「匿名使用报告」。项目不发起
   任何除「抖音页面」和「你配置的通知通道」之外的网络请求。
2. **凭据只在本地**。登录态文件永远只写在本机 `data/accounts/`，不加密上传、
   不云同步、不经过任何第三方。
3. **日志里不会出现敏感内容**。Cookie 值、令牌、通知密钥在写日志前都会被打码。
   如果你发现哪里漏了，那就是漏洞，请报告。
4. **备份功能默认加密**。`deploy/BACKUP.md` 里的示例命令都带 `openssl enc`。

## 部署者自查清单

如果你要把这个跑在公网服务器上，请确认：

- [ ] `HUOHUA_TOKEN` 是 32 位以上的随机串，不是 `123456`
- [ ] 已设置 `HUOHUA_ALLOWED_IPS`，或端口映射为 `127.0.0.1:8787:8787`
- [ ] 前面套了 HTTPS（`.env` 里的令牌和二维码都不该走明文）
- [ ] `data/` 目录权限不是 777，登录态文件是 600
- [ ] `.env` 不在 git 里（`.gitignore` 已默认排除，但别自己 `git add -f`）
- [ ] 服务器本身打了安全更新（`unattended-upgrades` 之类）
- [ ] 备份文件是加密的，且验证过能解开
- [ ] 知道怎么快速关掉服务（`docker compose down` / `systemctl stop`）

## 支持的版本

只有最新的 `main` 分支会收到安全修复。这个项目还没到 1.0，不维护旧版本分支。
